import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>MNIST Digit Recognition</h1>
            <p>Upload an image of a digit to get started!</p>
        </header>
    );
};

export default Header;
